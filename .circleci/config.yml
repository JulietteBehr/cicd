version: 2.1

jobs:
  save_hello_world:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: echo "Hello World" > output.txt

workflows:
  welcome:
    jobs:
      - save_hello_world